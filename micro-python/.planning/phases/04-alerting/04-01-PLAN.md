---
phase: 04-alerting
plan: 04-01
type: auto
autonomous: true
wave: 1
depends_on:
  - 03-visualization

objective: Implement automated alerting for high temperature events (>30°C) configurable via environment variables.

context:
  - backend/docker-compose.yml
  - backend/stack.env
  - backend/grafana/provisioning/

tasks:
  - type: auto
    name: Add ALERT_THRESHOLD to backend configuration
    description: |
      Update `backend/stack.env` and `backend/stack.env.example` to include `ALERT_THRESHOLD_TEMP=30`.
      Update `backend/docker-compose.yml` to pass this variable to Grafana.

  - type: auto
    name: Configure Grafana Notification Channel (Contact Point)
    description: |
      Create `backend/grafana/provisioning/alerting/contact_points.yml`.
      Set up a default contact point (e.g., email or webhook).
      Since no specific channel was requested, use a local logger or placeholder for now, 
      and ask the user for their preferred channel (Discord/Slack/Email) in a deviation or next step.

  - type: auto
    name: Provision Alert Rule
    description: |
      Create `backend/grafana/provisioning/alerting/rules.yml`.
      Define a rule that queries InfluxDB for `temperature > ${ALERT_THRESHOLD_TEMP}`.
      Note: Grafana provisioning supports variable expansion in some fields, but if not, 
      we might need to hardcode it or use a script. 
      *Actually*, Grafana provisioning files (.yml) do support env var expansion (`$ENV_VAR`).

verification:
  - Verify `ALERT_THRESHOLD_TEMP` exists in `stack.env`.
  - Verify Grafana loads the provisioning files on startup.
  - Verify the alert rule is visible in Grafana UI.

success_criteria:
  - [ ] Alert threshold is configurable via `stack.env`.
  - [ ] Alert rule exists in Grafana.
  - [ ] System is ready to send notifications (even if just to log for now).

output:
  - backend/grafana/provisioning/alerting/*.yml
  - Updated stack.env
---
